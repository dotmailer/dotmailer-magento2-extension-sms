<?xml version="1.0" encoding="UTF-8"?>
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="TransactionalSmsOrderFlowTest">
        <annotations>
            <features value="Dotdigital module"/>
            <stories value="dotdigital Transactional SMS"/>
            <title value="Test SMS order flow"/>
            <description value="Validate that order creation creates a queued SMS send."/>
            <severity value="MINOR"/>
            <group value="FS/mftf-130088"/>
            <group value="dotdigitalSms"/>
        </annotations>

        <before>
            <actionGroup ref="AdminLoginActionGroup" stepKey="AdminLoginActionGroup1"/>
            <comment userInput="Set default flat rate shipping method settings" stepKey="setDefaultFlatRateShippingMethodComment"/>
            <createData entity="FlatRateShippingMethodDefault" stepKey="setDefaultFlatRateShippingMethod"/>
            <!-- Create simple customer -->
            <comment userInput="Create simple customer" stepKey="createSimpleCustomerComment"/>
            <createData entity="Simple_US_Customer_CA" stepKey="simpleCustomer"/>
            <!-- Create the category -->
            <comment userInput="Create the category" stepKey="createCategoryComment"/>
            <createData entity="ApiCategory" stepKey="createCategory"/>
            <!-- Create Simple Product -->
            <comment userInput="Create Simple Product" stepKey="createSimpleProductComment"/>
            <createData entity="simpleProductDefault" stepKey="simpleProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>

            <actionGroup ref="SetDotmailerAccountData" stepKey="SetDotmailerAccountData"/>
            <actionGroup ref="EnableSms" stepKey="EnableSmsOptions" />

            <!-- Assign default source to simple product -->
            <actionGroup ref="DotAdminAssignDefaultSourceToSimpleProductsActionGroup" stepKey="bulkAssignSourcesToAllProducts">
                <argument name="product1Sku" value="$$simpleProduct.sku$$"/>
                <argument name="source1_code" value="default"/>
            </actionGroup>

            <!-- Reindex data -->
            <actionGroup ref="CliIndexerReindexActionGroup" stepKey="performReindex">
                <argument name="indices" value="cataloginventory_stock inventory"/>
            </actionGroup>

            <actionGroup ref="CreateOrderActionGroup" stepKey="createNewOrder">
                <argument name="product" value="$$simpleProduct$$"/>
                <argument name="customer" value="$$simpleCustomer$$"/>
            </actionGroup>
        </before>

        <after>
            <deleteData createDataKey="simpleCustomer" stepKey="deleteSimpleCustomer"/>
            <deleteData createDataKey="simpleProduct" stepKey="deleteSimpleProduct"/>
            <deleteData createDataKey="createCategory" stepKey="deleteCategory"/>
            <actionGroup ref="EnableSms" stepKey="DisableSmsOptions">
                <argument name="enabled" value="No"/>
            </actionGroup>
            <actionGroup ref="ResetDotmailerAccountData" stepKey="resetAccountData"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <!-- Verify SMS message was queued -->
        <comment userInput="Navigate to message queue and verify SMS message exists" stepKey="checkQueueComment"/>
        <amOnPage url="{{AdminMessageQueuePage.url}}" stepKey="goToMessageQueuePage"/>
        <waitForPageLoad stepKey="waitForQueuePage"/>
        <actionGroup ref="AdminGridFilterSearchResultsByInput" stepKey="filterByTopicName">
            <argument name="selector" value="AdminMessageQueueFilterSection.topicName"/>
            <argument name="value" value="ddg.sms.message"/>
        </actionGroup>
        <seeElement selector="{{AdminMessageQueueSection.textInGridRow('Pending')}}" stepKey="seeStatusPending"/>

    </test>
</tests>


